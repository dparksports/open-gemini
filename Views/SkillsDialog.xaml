<ContentDialog
    x:Class="OpenClaw.Windows.Views.SkillsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenClaw.Windows.Views"
    xmlns:skills="using:OpenClaw.Windows.Services.Skills"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Skills Manager 🧩"
    PrimaryButtonText="Close"
    CloseButtonText="Reload Skills"
    DefaultButton="Primary">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Extend your agent with community scripts." Margin="0,0,0,10" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Gray"/>

        <ListView Grid.Row="1" ItemsSource="{x:Bind Skills}" Height="300" Width="400" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="skills:ISkill">
                    <Grid Margin="0,5">
                         <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="⚡" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{x:Bind Name}" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind Description}" FontSize="12" Foreground="Gray" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
